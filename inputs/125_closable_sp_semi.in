
% #125 Closable semilattice pseudo-complemented semigroup

formulas(assumptions).

% semilattice pseudo-complemented semigroup
(x * y) * z = x * (y * z).
x' * x = 0.
x * 0' = x.
x' * y' = y' * x'.
x' * (x' * y)' = x' * y'.
%
x'' * x = x.

end_of_list.

