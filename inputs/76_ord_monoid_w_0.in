
% #76 Ordered monoids with zero


formulas(assumptions).

% ordered monoid
(x * y) * z = x * (y * z).
x * 1 = x.
1 * x = x.
x < x.
x < y & y < x -> x = y.
x < y & y < z -> x < z.
x < y -> ((x * z < y * z) & (z * x < z * y)).
x < y | y < x.
% zero
x * 0 = 0 & 0 * x = 0.

end_of_list.

